# navigator

A path-less router, based on a idea of a stack. Create to serve as a quick implementation of a prototype for a more complex solution.

How to:
1. Clone the repository
2. Run `npm i` to install the dependencies

`npm build` builds the application to the `./dist` folder

`npm test` lunches the tests